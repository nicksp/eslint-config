name: CI

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  lint:
    uses: nicksp/workflows/.github/workflows/test.yml@4b1950fadababbc1057c7b25f8edebe186cb8f7d # v1
